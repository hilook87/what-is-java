一，final数据

大总：
数据恒定不变。

小总：
1,一个永不改变的编译时常量。
2,一个在运行时被初始化的值，而你不希望它被改变。

分组：

基本数据类型，final,定义时对其赋值；——》使数值恒定不变；——》这个也是编译器常量；——》在编译时执行计算式，减轻了一些运行时的负担；

对象引用，final；——》无法把它改为指向另一个对象，但是对象其自身是可以修改；

带有static的编译期常量，将用大写字母表示，并使用下划线分隔各个单词；

不能因为某数据是final就认为在编译时可以知道它的值；
|
例如：在运行时使用随机生成的数值来初始化：
private final int i4 = rand.nextInt(20);
static final int INT_5 = rand.nextInt(20);


final定义为静态和非静态的区别——》在数值在运行时内被初始化时才会显现；



这里有三个概念：编译，运行，初始化；









